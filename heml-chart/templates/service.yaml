apiVersion: v1
kind: Service
metadata:
  name: {{ include "heml-chart.fullname" . }}
  labels:
    {{- include "heml-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "heml-chart.selectorLabels" . | nindent 4 }}
